# Report-10-4



---

# 📋 گزارش پروژه: کنترل LED از طریق بلوتوث (Bluetooth LED Control)

## ۱. مقدمه  
در این پروژه، یک **ماژول بلوتوث (HC-05)** به آردوینو متصل شده است. وقتی کاربر از طریق یک اپلیکیشن موبایل (مثل *Bluetooth Terminal*) کاراکتر `'1'` را ارسال کند، LED روشن می‌شود؛ و اگر `'0'` را ارسال کند، LED خاموش می‌شود. این پروژه نشان‌دهنده‌ی توانایی آردوینو در **ارتباط بی‌سیم و کنترل از راه دور** است.



---
»

## ۳. قطعات مورد استفاده  
- برد آردوینو Uno  
- ماژول بلوتوث **HC-05** 
- LED (در اینجا از LED onboard پین 13 استفاده شده)  
- برد برد و سیم‌های جامپر  

  یک گوشی هوشمند.

---

## ۵. کد برنامه (اسکچ)

```cpp
#include <SoftwareSerial.h>

SoftwareSerial MyBlue(10, 11);  // RX=10, TX=11
char flag;                      // ذخیره آخرین دستور دریافتی
int LED = 13;                  // پین LED

void setup() {
  Serial.begin(9600);          // ارتباط با کامپیوتر (برای دیباگ)
  MyBlue.begin(9600);         // ارتباط با ماژول بلوتوث
  pinMode(LED, OUTPUT);        // تنظیم پین LED به عنوان خروجی
  Serial.println("Ready to connect");
  Serial.println("Default password is 1234 or 0000");
}

void loop() {
  if (MyBlue.available()) {    // اگر داده‌ای از بلوتوث دریافت شده باشد
    flag = MyBlue.read();     // خواندن یک کاراکتر
  }

  if (flag == '1') {
    digitalWrite(LED, HIGH);
    Serial.println("LED On");
  } else if (flag == '0') {
    digitalWrite(LED, LOW);
    Serial.println("LED Off");
  }
}
```




## ۶. نحوه‌ی کارکرد (مرحله‌به‌مرحله)
1. آپلود کد به آردوینو.  
2. روشن کردن ماژول بلوتوث (چراغ آن چشمک می‌زند).  
3. در گوشی، به **تنظیمات بلوتوث** بروید و به ماژول (مثلاً `HC-05`) متصل شوید (رمز: `1234`).  
4. اپلیکیشن **Bluetooth Terminal** را باز کنید.  
5. در کادر ارسال، کاراکتر `'1'` را بفرستید → LED روشن می‌شود.  
6. کاراکتر `'0'` را بفرستید → LED خاموش می‌شود.  
7. وضعیت در Serial Monitor آردوینو نیز نمایش داده می‌شود.

---

## ۷. نتایج  
- LED به درستی بر اساس دستورات بلوتوث روشن/خاموش می‌شود.  
- ارتباط بلوتوث بدون مشکل برقرار می‌شود.  

---

#

---

